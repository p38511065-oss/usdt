Combined upgrade package based on your uploaded working zip.
Updated:
- Premium landing page
- Seller/admin workspace banner styling
- Improved dashboard/admin visual design
- Landing page still uses landing-home.js and existing main.js
- Existing working logic preserved as much as possible

Quote Slab Selection Fix:
- Available Quotes -> Sell Request now carries selected slab directly.
- Higher amount slabs and open-ended max amount slabs now remain selectable.
- Sell Request matching uses quote_slabs consistently instead of template min/max fallback.
- USDT/TRC20 active wallet is checked before order creation.

Merged fix:
- Quote slab selection bug fix preserved
- Deposit page UX improvements preserved
- Small inline copy icons for wallet and amount
- TX hash submit now shows smoother submitting/success feedback

- Added payout details View modal for seller and admin panels

- Fixed payout View buttons with inline modal trigger fallback

FULL FRONTEND REBUILD v1:
- Rebuilt landing page HTML structure, not just CSS
- Rebuilt seller login page HTML structure
- Rebuilt admin login page HTML structure
- Added rebuild-ui.css as the new visual system
- Seller/Admin dashboard existing IDs and logic preserved, with rebuilt visual layer
- main.js was preserved to avoid breaking working flows

Landing rates fix:
- Fixed landing-home.js Supabase quote_slabs loader
- Added fallback for min_amount / min_crypto_amount and max_amount / max_crypto_amount
- Added fallback rows instead of broken ₹0.00 / loading failed
- Uses USDT/TRC20 first, then broader USDT fallback

Seller dashboard mobile app rebuild:
- Rebuilt dashboard.html structure to match reference mobile dashboard layout
- Added seller-mobile-ui.css with app header, overview cards, quote cards, active order, payout preview, tracking, bottom nav
- Preserved all existing IDs and main.js logic for quotes, sell flow, payout, KYC, orders
- Added small JS bindings for seller display name, KYC status, active payout, and preview top rate

Seller mobile polish:
- Fixed horizontal overflow on mobile
- Online badge no longer goes outside screen
- Compact mobile header
- Quote cards made controlled horizontal scroll
- Bottom nav refined
- Tracking preview made mobile-safe

Landing mobile polish:
- Compact mobile topbar
- Reduced hero overflow and improved headline spacing
- Hid heavy coin visual on mobile to keep page cleaner
- Fixed rates table mobile columns
- Tightened Live Market section spacing

Admin slabs only fix:
- Removed fallback/sample/random quote rates from landing page
- Landing page now shows only admin-created USDT/TRC20 quote slabs
- If slabs cannot load, it shows a clear permission/admin slabs message instead of fake prices
- Seller preview top rate now uses admin-created quote_slabs only

Seller logout fix:
- Added Logout button inside seller mobile menu drawer
- Removed hidden floating logout duplicate
- Existing logout button ID preserved for main.js logout logic

Referral system update:
- 0.10% referral reward on every completed referred sell order
- Reward generated when admin marks order completed
- Seller referral wallet with total, available, pending, paid
- Minimum referral withdrawal ₹2,000
- Seller can request withdrawal
- Admin Referral Rewards section with rewards and withdrawal management
- Added referral-system-migration.sql

Admin panel emergency fix:
- Fixed JavaScript syntax error caused by duplicate loadKycSection function text
- Admin panel working restored

Seller auth full fix:
- Rebuilt seller login/register JS flow
- Referral code from URL auto-fills register form
- Register creates/repairs seller profile when possible
- Login also repairs missing seller profile
- Dashboard no longer breaks if profile row was missing
- Added seller-auth-fix-policies.sql for profile RLS/register permissions
- Fixed auth tab panel visibility

KYC final fix:
- Compresses KYC images before saving to avoid mobile large-image submit failure
- KYC submit button now shows Submitting and clear error/success
- Address no longer blocks submit
- Adds schema-cache fallback for optional columns
- Added kyc-final-database-fix.sql

KYC RPC final fix:
- Seller KYC submit now calls Supabase RPC submit_kyc()
- RPC repairs missing profile row and inserts/updates KYC with security definer
- Direct table insert remains fallback
- Added kyc-rpc-final-fix.sql

Seller button binding fix:
- Added dashboard fallback script that captures Save Payout Method and Submit KYC clicks
- If old main.js binding fails, these buttons still show loading/errors and submit
- KYC uses submit_kyc RPC
- Payout saves directly to bank_accounts

Seller all actions fallback:
- Added capture fallback handlers for seller buttons beyond KYC/Payout
- Refresh Quotes and Show Quotes now have fallback logic
- Confirm Sell Request fallback creates sell_orders
- Referral withdrawal, copy code/link, reset payout, logout fallback added
- If main.js event binding fails, seller buttons should still show messages/actions

Referral final fixed/merged with latest seller all-buttons build.

Seller TRC20 default update:
- Seller side network is now fixed/default TRC20.
- sell-network and quotes-network fields are readonly TRC20.
- Seller quote/sell logic uses TRC20 only.

Seller UX cleanup:
- Bottom nav New Quote replaced with Sell USDT.
- Available Quotes renamed to Rate Board.
- Sell Crypto renamed to Sell USDT.
- Seller coin/network are fixed readonly USDT/TRC20.
- Added helper text to Rate Board and Sell USDT sections.
- Seller flow wording changed: Get Best Admin Rate → Create Sell Order.

Editable amount update:
- Rate Board Use This Rate may auto-fill minimum slab amount as a starting value.
- Sell amount remains editable, so seller can enter any amount within admin slab range.
- Added clear help message explaining amount can be edited.
- Coin remains USDT and network remains TRC20.

Payout details display fix:
- Seller payout table now shows holder name, UPI ID, bank name, account number and IFSC.
- Selected payout summary now shows full details.
- New sell orders now save flat payout fields too.
- Admin order payout section now shows holder name, UPI/bank, account and IFSC directly.
- Added payout-details-final-fix.sql for database columns/backfill.

Admin Orders pagination:
- Orders & Payouts now shows latest 5 orders per page.
- Page numbers 1,2,3... added below orders table.
- Search/filter resets pagination to page 1.
- Order detail/timeline/payout details remain unchanged.

Admin premium mobile UI update:
- Added mobile-friendly admin dashboard inspired by reference image.
- Added premium header, stat cards, quick section tabs, recent order card, order steps, quick actions and bottom mobile navigation.
- KYC button removed from recent order; order actions are Crypto Received, Start Payout and Mark Paid.
- Existing admin sections and working logic preserved.

Admin Premium UI v2:
- Strong CSS override added so old admin design does not show.
- Sidebar hidden and app-style mobile/desktop dashboard shown.
- Dashboard header/cards/tabs/recent order/bottom nav styled closer to reference image.

Admin Orders mobile fit patch:
- Orders & Payouts table converts into mobile cards under 760px.
- Order detail, payout, wallet and timeline sections become single-column on mobile.
- Buttons and pagination are mobile-friendly.
- No backend/logic changes.

Admin menu/notification fix:
- Top menu button opens admin menu dropdown.
- Logout option added inside floating menu.
- Notification icon opens notification panel.
- Dropdowns close on outside click.

Seller order summary flow update:
- Sell page now has separated stages: sell details, admin rate, order summary/payment, tracking after TX.
- Order tracking is hidden until TX hash is submitted.
- QR and wallet address now appear inside Order Summary & Payment section, not timeline.
- Payment submit success then shows tracking section.

Seller new order reset fix:
- Old order summary/payment card now hides when seller changes amount or payout method.
- Old tracking card hides when starting a new sell order.
- Get Best Admin Rate resets previous order state before showing fresh quote.
- Rate Board selection resets previous payment/tracking state before new order flow.

Seller active order lock:
- If seller has any incomplete latest order, Sell USDT page shows that active order instead of new order form.
- New order creation is blocked until last active order is completed/cancelled/rejected.
- Once order is completed, Sell USDT flow unlocks and can start fresh.

Seller cancel pending order:
- Seller can cancel active order before TX hash is submitted.
- After cancellation, Sell USDT form unlocks and user can start again.
- If TX hash exists, seller cannot self-cancel and must contact admin.
- Added seller-cancel-order-policy.sql.

Duplicate TX hash fix:
- TX hash is trimmed before submit.
- Seller side checks if TX hash already exists in another order.
- Duplicate TX hash shows clear error message.
- Added database unique index in tx-hash-duplicate-fix.sql.

Referral working + mobile fix:
- Referral code captured from ?ref= and stored until profile creation.
- Seller profile now resolves referred_by from referral_code.
- Referral rewards generate after admin marks referred seller order completed/paid.
- Added referral-working-final-fix.sql with tables, policies, code backfill and old completed reward backfill.
- Referral seller page mobile layout improved.

Referral referred seller names:
- Added Referred Sellers list on seller referral page.
- Shows referred seller name, email/mobile, user status, KYC status and joined date.
- Referral rewards table now also shows referred seller name, email and mobile.
- Mobile cards added for referred sellers.

Telegram support update:
- Replaced duplicate bottom nav Sell USDT with Support.
- Added seller Support section with Telegram button.
- Added Telegram support link in order payment step.
- Added support button in Profile support notes.
- Use @anmolaro placeholder and replace with real Telegram username/link.

Telegram support real link update:
- Telegram support link changed to https://t.me/anmolaro
- Telegram username changed to @anmolaro

Telegram support visible fix:
- Correctly inserted seller Support section before Profile.
- Fixed bottom nav Support label visibility.
- Added robust copy handler for @anmolaro.

Support icon update:
- Changed Support icon from airplane to headset for clearer support/help meaning.

Final cleanup patch:
- Added landing page footer with Telegram support, legal/risk links and disclaimer.
- Added admin confirm prompts for order status, KYC and referral actions.
- Removed visible hardcoded rate placeholders from landing/admin text where possible.
- Added small toast UI for selected seller success messages.
- Cleaned seller cancel SQL file to avoid invalid 'created' enum issue.

Referral withdrawal payout method update:
- Seller can choose payout method before referral withdrawal request.
- Selected payout details preview is shown before request.
- Withdrawal request saves payout_method_id, payout_label and payout_details.
- Seller withdrawal history now shows payout method.
- Admin referral withdrawals now show payout method details with View Payout button.

Referral withdrawal balance lock fix:
- Available referral balance now subtracts all non-rejected withdrawals including requested/approved/processing/paid.
- Seller cannot submit a new withdrawal while one request is active.
- Withdrawal button shows pending state until admin action.
- Added optional SQL unique index: referral-withdrawal-active-lock.sql.

Referral withdrawal strong lock:
- Submit now re-fetches latest rewards/withdrawals from database before allowing request.
- Active pending withdrawal blocks new request even if page had old balance.
- Database SQL added to reject duplicate active requests and create unique active-withdrawal lock.

Referral withdrawal final balance guard:
- Withdrawal button now disables when available balance is below ₹2,000.
- Fresh database balance is checked again before insert.
- Added SQL trigger to block withdrawal if amount is greater than real available balance.
- Trigger also blocks multiple active withdrawal requests.

Referral remaining balance UI:
- Available card renamed to Remaining Balance.
- Paid card renamed to Withdrawn / Locked.
- Shows Total Earned, Withdrawn/Locked and Remaining breakdown.
- Remaining Balance stays visible even if it is below ₹2,000.

Referral withdrawal simple balance logic:
- Removed active withdrawal blocking.
- Remaining balance is always Total Rewards minus all non-rejected/cancelled withdrawal requests.
- User can create multiple withdrawal requests only while remaining balance is enough.
- Added SQL trigger that blocks withdrawal amount greater than remaining balance.
- Added SQL to drop old active-withdrawal unique index.

Referral remaining balance card final fix:
- Remaining Balance card now uses Total Rewards minus Total Withdrawal Requests directly.
- Supports alternate reward amount column names safely.
- Withdrawal validation re-fetches fresh rewards/withdrawals and blocks above remaining balance.
- Added final simple balance SQL trigger.

Referral pagination update:
- Seller referral rewards list now shows 5 per page with page numbers.
- Seller referral withdrawal requests now show 5 per page with page numbers.
- Admin referral rewards list now shows 5 per page with page numbers.
- Admin referral withdrawal requests now show 5 per page with page numbers.

Referral pagination safe fix:
- Fixed admin referral history blank issue caused by wrong variable/ID names.
- Admin referral rewards and withdrawals now use safe pagination, 5 per page.
- Corrected admin referral reward relation alias to referred_user.
- Added correct pagination containers in admin.html.
- Seller referral pagination kept intact.

Seller volume stats update:
- Seller dashboard now shows Total USDT Volume.
- Seller dashboard now shows Total INR Volume.
- Completed Volume also shows completed USDT amount.
- Latest order card now includes USDT Amount.

Seller dashboard volume + sell cleanup:
- Added seller Total USDT Volume, Total INR Volume and Completed Volume.
- Sell USDT section visually redesigned as a cleaner 4-step checkout flow.
- Coin/network fields kept for JS but shown as compact USDT/TRC20 chips to reduce confusion.

One-shot full update:
- Seller dashboard volume cards added: Total USDT Volume, Total INR Volume, Completed Volume.
- Sell USDT section converted into internal step screens: Details, Rate, Payment, Tracking.
- Landing page lightweight animation added: hero glow, card fade-up, CTA shine.
- Admin filters/search added for Orders, KYC and Referral section.
- Admin notification badge now updates from pending orders, KYC and referral withdrawals.
- No SQL changes required.

Sell step screen fix:
- Get Best Admin Rate now switches to Rate screen reliably.
- Create Sell Order now switches to Payment screen reliably.
- Active order opens Payment/Tracking screen correctly.
- Fixed hidden-flow-card CSS conflict for active step screens.

Sell flow final no-conflict fix:
- Removed strict hide/show step-screen dependency that was blocking next section.
- Get Best Admin Rate now scrolls to rate section.
- Create Sell Order now reveals and scrolls to Payment section.
- TX submit reveals and scrolls to Tracking section.

Practical final updates:
- Added admin CSV export buttons for Orders, KYC, Referral Rewards and Referral Withdrawals.
- Added seller My Orders mobile card layout while keeping desktop table.
- Added landing page trust/safety content section.
- Made admin notification panel items clickable shortcuts.
- No SQL changes required.

Seller dashboard TRC20 quote preview:
- Removed ERC20 / Other card from seller dashboard.
- Added Admin Quote Slabs preview for USDT / TRC20.
- Quote slabs are scrollable if many rates exist.
- Clicking a slab prefills Sell USDT form.

Dashboard rate range fix:
- Seller dashboard no longer shows highest quantity rate as single Best Rate.
- Preview now shows USDT/TRC20 rate range: lowest to highest admin slab.
- Note explains higher quantity may get higher rate.
- Admin slabs preview still shows each quantity range with its own rate.

Payout page UI fix:
- Replaced large broken checkbox with clean toggle switch.
- Bank/UPI fields hide/show properly.
- Added primary payout preview in setup notes card.
- Added mobile card layout for saved payout methods.
- Added bottom spacing so bottom nav does not cover saved methods.

Final polish update:
- Seller active/latest order card upgraded with Continue/Track and Details actions.
- Added seller order detail modal with order summary, payout, wallet, TX and tracking.
- Admin dashboard stats upgraded: today volume, pending payout amount, completed today, pending KYC and referral withdrawal count.
- Added admin order detail modal.
- Landing page copy polished for USDT TRC20 only, admin slab rates, manual verification, KYC, TX protection and support.
- Added VERSION-NOTES.txt as stable backup note.
- No SQL changes required.

Admin recent orders fix:
- Restored missing adminMiniStep and adminOverviewStep helper functions.
- Recent Orders section now renders latest order instead of staying blank.
- Added fallback styling for recent order timeline steps.

Admin dashboard Crypto Received button fix:
- Added dedicated updateOverviewOrderStatus handler for Recent Orders dashboard card.
- Crypto Received, Start Payout and Mark Paid buttons now update order status and refresh dashboard.
- Button enable/disable rules are clearer by current order status.

Batch slots + USDT capacity update:
- Added seller dashboard and Sell USDT batch banners.
- Added admin batch/order slots control in dashboard overview.
- Admin can create new batch, add more slots, add more USDT capacity, pause/resume, close batch.
- Seller order creation checks active batch, remaining order slots and remaining USDT capacity.
- Added waitlist support for full/paused batches.
- Requires SQL: order-batches-slots-usdt-capacity.sql.

Batch name preset update:
- Batch name now has preset dropdown: Morning Batch, Afternoon Batch, Evening Batch and Custom Batch.
- Custom Batch reveals a custom name input.
- Admin no longer needs to type common batch names manually.

Batch default message update:
- Batch Message field now loads with default text: USDT/TRC20 batch live now.
- JS fallback also uses the same default message.

Batch count trigger fix:
- Removed frontend used_orders/used_usdt update to avoid permission issues and double counting.
- Added SQL trigger to automatically increment used_orders and used_usdt when sell_orders row is inserted.
- Added SQL trigger to return slot/capacity when order is cancelled/rejected.
- Improved seller batch banner premium highlight.
- Requires running updated order-batches-slots-usdt-capacity.sql.

Final premium batch banner update:
- Batch banner heading is larger and clearer.
- Batch message, slots left and USDT capacity are split into separate highlighted areas.
- Sell USDT CTA is larger and more visible.
- Mobile layout stacks cleanly.
- Includes previous batch trigger count fix.

Final bug fixes:
- Batch banner Sell USDT Now button works via delegated side-link handler.
- KYC approve/reject actions are hidden after final status, preventing approved KYC from being rejected again.
- Referral reward/withdrawal actions are hidden after paid/rejected final status.
- Admin Recent Order timeline no longer shows Crypto Received as ticked just because seller submitted TX; it stays current until admin flow progresses.

Premium Smart Trust Ticker update:
- Added premium trust ticker to landing page and seller dashboard.
- If completed orders are 5 or more, ticker shows masked real seller success messages.
- If completed orders are fewer than 5, ticker shows generic trust/batch/payment messages to avoid repeating one name.
- No full names, phone numbers, or private payout details are shown.
- No SQL required.

Batch waitlist clear + feedback update:
- Creating a new batch now clears old waitlist entries by marking them batch_opened.
- Waitlist table now only shows active waiting entries.
- Batch control buttons show Processing... while running.
- Batch status message clearly shows success/error after every action.

Realtime + landing ticker fix:
- Seller dashboard now subscribes to live database changes for orders, batches, KYC, referral, withdrawal and payout methods.
- Admin dashboard now subscribes to live changes for orders, batches, KYC, referrals, waitlist, payout methods, slabs and wallets.
- Live refresh uses debounce to avoid flickering.
- Landing Smart Trust Ticker now renders generic messages immediately and no longer stays stuck on Loading.
- Added realtime-live-updates.sql for Supabase Realtime publication.

Landing ticker hard fix:
- Added immediate landing ticker fallback before Supabase config check.
- Added inline landing fallback script directly in index.html so ticker never remains on Loading.
- Landing ticker now works even if Supabase config/realtime is delayed or blocked.
- Existing Supabase completed-order ticker can still update it later.
